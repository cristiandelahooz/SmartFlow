# Lombok configuration for SmartFlow project

# Configure the logger field name (default is 'log')
lombok.log.fieldName = log

# Ensure logger field is static (default is true)
lombok.log.fieldIsStatic = true

# Stop bubbling - don't search parent directories for lombok.config
config.stopBubbling = true